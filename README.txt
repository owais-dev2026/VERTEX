# Owais Studio — Social Media Design Portfolio

1. Open `index.html` locally to preview.
2. For GitHub Pages, upload `index.html` and the `images` folder.
3. Replace `hello@example.com` in `index.html` with your real contact method if desired.
